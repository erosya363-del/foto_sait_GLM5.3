# СТАТУС ПРОЕКТА «Фотокаталог» (Askona) v1.5

Обновлено: 13 сентября, после Шага 4. Полный журнал — `worklog.md`.
**Защита от откатов: всё закоммичено в git (коммит `351c1a8`).**

---

## ГДЕ ОСТАНОВИЛСЯ (точка возобновления)

**Шаг 4 выполнен и проверен E2E (32/32 ✓). ЖДУ ПРОВЕРКУ ПОЛЬЗОВАТЕЛЯ на iPhone.**

⚠️ Важно: из-за отката песочницы пользователь мог видеть промежуточную версию.
Перед проверкой: **удалить ярлык PWA с Дома и создать заново** (iOS кэширует манифест/сплэш).

После «да» → **Шаг 5** (админ: физическое удаление, корзина, лёгкое создание контента).

---

## ЧТО СДЕЛАНО

### Инцидент восстановления (до Шага 4)
Откат песочницы к коммиту 14:12 уничтожил Шаги 1–3 и фикс полосы (исходники, бэкап, билд).
**Всё восстановлено в точности по записям worklog** и проверено E2E; затем всё закоммичено в git —
повторные откаты больше не опасны.

### Шаг 1 ✅ (восстановлен)
Капсульная навигация, фон html без белой полосы, круглая «Назад» 40px,
пружинный сегмент-контрол складов (Обухово/Владимир) в «Остатках».

### Шаг 2 ✅ (восстановлен)
Повторный тап «Каталог» = мгновенный возврат наверх + отклик:
пульс капсулы (.nav-pulse) + tick-звук (Web Audio) + вибрация (Android; iOS — звук+пульс).

### Шаг 3 ✅ (восстановлен)
Крошки-путь «Каталог › Диваны › Магни › Sky 01» на всех уровнях + в шапке товара
(круглая «Назад» 40px), тап по любому звену = мгновенный переход, прыжок из открытого
товара (jumpProductToLevel), автоскролл ленты к текущему звену.

### Фикс полосы PWA ✅ (фикс v3 «плоский низ», восстановлен)
Механизм: iOS раскладывает standalone-PWA во вьюпорт без нижнего safe-area (~47pt),
буквбокс красится плоским фоном html. 4 слоя защиты:
1. `html { background: var(--edge) }` (#10161a тёмная / #eef3f4 светлая)
2. `.fx-skirt` — последние 48px вьюпорта = плоский `--edge` + 48px растушёвка
3. `.boot-bg` — та же юбка на сплэше
4. Nudge-серии пересчёта вьюпорта: до гидратации (layout.tsx, 120–3000мс + pageshow)
   и после (BootSplash, 150–3200мс + touchend + visibilitychange)
5. Градиенты запечены в фон body, fixed-авроры ослаблены вдвое (сумма та же)
6. `manifest background_color` → #10161a

### Шаг 4 ✅ НОВОЕ (проверено E2E 32/32)
- **Лента «Новинки за 7 дней»** на главном экране каталога: свежие фото (горизонтальный
  скролл, бейдж NEW на каждом), тап → открытый товар. API: `/api/catalog?level=fresh`.
- **Кнопка «Все ткани»** на уровне 1 (брендовая карточка с счётчиками) → экран всех тканей
  (карточки с превью, счётчиками вариантов и NEW) → тап по ткани → все её варианты фото.
  API: `/api/catalog?level=fabrics`. Полная навигация: крошки «Каталог › Все ткани › Ткань»,
  «Назад», браузерный Back, режимы вида.
- **NEW-бейджи**: варианты/фото, добавленные за 7 дней — пилюля NEW на карточках
  (каталог, поиск, ткани), бейдж на моделях с новинками, счётчик «· N новых» на тканях.
  API: `isNew` в /api/catalog и /api/search, `newCount` на уровне моделей.

E2E: 32 проверки — лента, «Все ткани», крошки, прыжки из товара, повторный тап «Каталог»,
сегмент-контрол складов, CSS юбки/html-bg, консоль чистая. Скриншот: scripts/s4-l1-final.png.

---

## ЧТО ОСТАЛОСЬ

| Шаг | Содержание | Статус |
|-----|-----------|--------|
| 1–3 | v1.5 + отклик + крошки + фикс полосы | ✅ принято/восстановлено |
| 4 | «Все ткани», NEW-бейджи, лента за 7 дней | ⚠️ ждёт проверки (пересоздать ярлык!) |
| 5 | Админ: физическое удаление, корзина, лёгкое создание контента | ⏳ следующий |
| — | Проверка полосы/Шага 4 на Android | по возможности |

---

## КОПИЯ SAIT_COPY_1 (бэкап)

- **Копия:** `/home/z/my-project/sait_copy_1/` (141M; без node_modules/.next/логов;
  включены исходники, db/custom.db, .env, .git, prisma, public, scripts, tests, upload)
- **Архив:** `/home/z/my-project/download/sait_copy_1.tar.gz` (114M)
- **Восстановление:** сказать «возвращай копию» → `scripts/restore_copy.sh`
  (rsync-зеркало → bun install → build → перезапуск :3000)
- Теперь бэкап синхронизируется после каждого шага И коммитится в git.

## ИНФРАСТРУКТУРА (важно!)

- Production: `node .next/standalone/server.js`, порт 3000. Dev-сервер НЕ ДЕРЖАТЬ.
- **Сервер убивается между сообщениями — стартовать+тестировать одной командой.**
- Сборка: `bun run build`. E2E: `scripts/test-s4.mjs` (32 проверки).
- Ключевые файлы: `src/components/breadcrumbs.tsx`, `catalog-view.tsx`, `portal.tsx`,
  `warehouse-segmented.tsx`, `boot-splash.tsx`, `src/lib/tick.ts`, `store.ts`,
  `src/app/api/catalog/route.ts`, `globals.css`, `layout.tsx`, `public/manifest.webmanifest`.
